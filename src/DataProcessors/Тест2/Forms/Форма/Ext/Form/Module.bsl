
&НаКлиенте
Процедура ТестВопросовЛапша(Команда)
	
КонецПроцедуры

&НаКлиенте
Процедура ТестВопросовКонвейер(Команда)
	
	ОчиститьСообщения();
	
	Конвейер = КонвейерЗаданийКлиент;
	
	// Коробка, которая двигается по конвейеру через все этапы (см. КонтекстЗадания.ОбщиеПараметры)
	ОбщиеПараметры = Новый Структура;
	ОбщиеПараметры.Вставить("НужноПоздороваться", Ложь);
	
	// Обработчик ошибок, возникших на конвейере
	ОбработчикОшибок = Конвейер.ОбработчикОшибок("ОбработчикОшибок", ЭтотОбъект);
	
	// Формирование этапов конвейера и запуск (в стиле fluent)
	
	Линия = Конвейер.СоздатьЛинию();
	
	Конвейер.ДиалогВопроса(
		Линия,
		Новый ОписаниеОповещения("ОбработатьРеакциюНаВопрос", ЭтотОбъект, ОбщиеПараметры),
		"Поздороваться?",
		РежимДиалогаВопрос.ДаНет
	);
	
	Конвейер.НачалоУсловногоБлока(Линия, "ПроверкаУсловия", ЭтотОбъект, ОбщиеПараметры);
	
		Конвейер.ПроизвольноеЗадание(
			Линия,
			"ЗаданиеПриветствие",
			ЭтотОбъект,
			ОбщиеПараметры
		);
		
	Конвейер.ОкончаниеУсловногоБлока(Линия);
	
	Конвейер.НачатьВыполнение(
		Линия,
		ОбработчикОшибок,
		Неопределено,
		ОбщиеПараметры,
		"Обработка.Тест2.Форма.ТестВопросовКонвейер"
	);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьРеакциюНаВопрос(Результат, ОбщиеПараметры) Экспорт
	
	Если Результат = КодВозвратаДиалога.Да Тогда
		
		ОбщиеПараметры.НужноПоздороваться = Истина;
		
	КонецЕсли; 
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверкаУсловия(Результат, ОбщиеПараметры) Экспорт
	
	Результат = ОбщиеПараметры.НужноПоздороваться;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаданиеПриветствие(Результат, ОбщиеПараметры) Экспорт
	
	Сообщить("Привет!");
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработчикОшибок(КонтекстОшибки, ДополнительныеПараметры) Экспорт
	
	Сообщить(СтрШаблон("Ошибка: Текст = ""%1""; НужноПоздороваться = ""%2""", КраткоеПредставлениеОшибки(КонтекстОшибки.ИнформацияОбОшибке), КонтекстОшибки.ОбщиеПараметры.НужноПоздороваться));
		
КонецПроцедуры

